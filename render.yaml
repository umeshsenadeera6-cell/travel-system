services:
  - type: web
    name: travel-system-backend
    runtime: node
    plan: free
    rootDir: ./
    buildCommand: npm run build
    startCommand: npm start
    healthCheckPath: /
    envVars:
      - key: MONGODB_URI
        sync: false
